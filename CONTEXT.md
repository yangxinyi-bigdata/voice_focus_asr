# Domain Context

本文定义 Voice Focus ASR 的稳定领域术语。代码、协议和文档应优先使用这些名称。

## 目标说话人

用户希望持续关注并转写的已知说话人。通常通过一段预注册的干净语音建立声纹。

避免使用“当前用户”作为同义词，因为目标说话人可能不是设备持有者。

## 目标声纹样本

用于表征目标说话人身份的预录音频。它只用于说话人增强、验证或目标说话人提取，不作为 ASR 训练数据。

## 说话人日志（Speaker Diarization）

判断“谁在什么时候说话”，输出说话人标签和时间区间。它不保证从重叠语音中恢复独立波形。

避免把 diarization 称为“独立音轨分离”。

## 语音分离（Speech Separation）

从混合波形估计多个独立说话人波形。输出是一个或多个音轨，不等同于说话人身份识别。

## 目标说话人提取（Target Speaker Extraction）

根据目标声纹样本，从混合语音中恢复目标说话人的波形。它解决“只听目标用户”的核心问题。

## 说话人增强（Speaker Enhancement）

云端或本地系统对目标说话人进行优先检测并抑制旁人及背景噪声的能力。它可能只影响识别或轮次判断，不保证输出可保存的独立音轨。

## 流式语音识别

持续接收音频分片，并产生可修订的增量文本和不可再修改的最终文本。

## 聚焦转写会话

从目标声纹准备开始，经过音频处理和流式语音识别，直到正常完成或中止的一次完整运行。

## 关系

- 一个聚焦转写会话必须对应一个目标说话人。
- 一个目标说话人可以拥有多个目标声纹样本。
- 目标说话人处理位于流式语音识别之前。
- 说话人日志可以作为观测或评测能力存在，但不能替代目标说话人提取。
- 降噪、回声消除和 VAD 是音频前处理，不拥有转写会话状态。
