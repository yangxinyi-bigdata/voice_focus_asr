# Voice Focus ASR：场景、术语与领域约定

本文定义 Voice Focus ASR 的稳定领域术语。代码、协议和文档应优先使用这些名称。

## 使用场景

用户佩戴助听器，在餐馆等嘈杂环境中与一位对话对象面对面聊天，经常听不清对方说话。系统只把**对方**说的话实时转成文字，第一阶段显示在手机上，后续显示在智能眼镜上。

本项目的目标是**忠实转写目标说话人的全部对话内容**，不判断话语是不是给系统的指令，也不执行指令。

- 对方佩戴无线领夹麦克风（如 DJI Mic 系列），拾音属于近场。
- 用户自己的声音、邻桌人声和餐馆环境声都是干扰。
- 优先保证文字准确，允许约 2–3 秒显示延迟；推理可以放在云端 GPU。

远场、多人重叠会议录音（如 REAL-T）只作为辅助评测，不代表主场景。

下面解释[方案分析](./docs/experiments/target_speaker_options_2026-09-22.md)、[项目说明](./README.md)中出现的英文、缩写、技术名词与模型名称。先看模型与工具名称，再看餐馆例子；后面可当词典查。本文件开头的“使用场景”是当前项目的主场景。

## 名字很长的东西：哪些是模型，哪些是工具

下表中的“模型”是机器学习程序；“工具包”是运行或组合模型的软件；“数据集”是一批带参考答案的录音。名字里有 ASR、TSE、VAD 等缩写时，可看后面的“最值得先认识的十个词”，不用猜名字每个字母的来历。

| 名称 | 它是什么 | 在这次讨论中做什么 |
| --- | --- | --- |
| **[Xiaomi-CocktailASR-1](https://github.com/xiaomi-research/xiaomi-cocktailasr-1)** | 小米发布的目标说话人识字模型。“Cocktail”借用多人交谈的“鸡尾酒会问题”说法。 | 输入混合录音和目标参考声音，直接尝试输出目标文字；现阶段先测整句质量。 |
| **PS4** | 本项目已试验的目标音轨提取模型名称；这里不是游戏主机。 | 先尝试提取目标声音，再送给识字模型；已有目标缺席时误输出旁人的反例。 |
| **[Qwen-Audio-Realtime](https://help.aliyun.com/zh/model-studio/fun-audiochat-realtime-websocket-api)** | 千问的实时音频服务。 | 本项目现有探针借助声纹与轮次判断，观察是否只输出目标人的文字；它不交付独立音轨。 |
| **[Qwen3-ASR](https://github.com/QwenLM/Qwen3-ASR)** | 千问的语音转文字模型系列。 | 可作为识字后端或参考模型；名字中的 ASR 仍是“语音识别”。 |
| **[pyannote Community-1](https://github.com/pyannote/pyannote-audio)** | 开源分说话人工具包中的一个模型组合。 | 给录音标注“匿名说话人几时在说话”，之后还须确认谁是目标。 |
| **[FunASR](https://github.com/modelscope/FunASR)** | 汇集语音识别及相关组件的工具包。 | 可组成中文识字、检测人声、标注说话人的测试链路。 |
| **Paraformer** | FunASR 生态中的识字模型。 | 把中文语音转成文字；部分版本提供分片输入。 |
| **[SenseVoice](https://github.com/FunAudioLLM/SenseVoice)** | 另一种音频识别模型系列。 | 可作为中文识字对照；本身的名字不表示“已经认出目标人”。 |
| **FSMN-VAD** | 一种普通人声活动检测模型。 | 辅助找出有语音的区间，仍不判断是不是目标人。 |
| **[CAM++ / ERes2NetV2](https://github.com/modelscope/3D-Speaker)** | 两种说话人特征模型名称。 | 将声音变成便于比较身份的声纹向量；其内部名称不需要展开才能使用。 |
| **3D-Speaker** | 提供声纹识别与说话人相关模型的项目名。 | CAM++、ERes2NetV2 等模型的来源。 |
| **[WeSep](https://github.com/REAL-TSE/wesep-real-tse)** | 目标说话人提取工具包。 | 作为 PS4 的音轨提取对照；`causal` 版本有利于测试短时处理。 |
| **[ClearerVoice-Studio](https://github.com/modelscope/ClearerVoice-Studio)** | 声音增强与分离工具包。 | 可试“两轨分离后匹配目标”的路径。 |
| **MossFormer2_SS_16K** | ClearerVoice 中的纯音频、两说话人分离模型。`SS` 可记为 speech separation。 | 输出两条音轨；还要判断哪条属于目标人。 |
| **AV_MossFormer2_TSE_16K** | ClearerVoice 中需要声音和视频的目标提取模型。`AV` 是 audio-visual，音视频。 | 用目标人的视频信息辅助提取，不能只给一段声纹录音使用。 |
| **[nomo-pvad](https://github.com/tuya/nomo-pvad)** | 目标人是否正在说话的检测模型。 | 控制何时关注录音；每 160 ms 给一个活动分数，不输出干净目标音轨。 |
| **[gpt-4o-transcribe-diarize](https://developers.openai.com/api/docs/guides/speech-to-text#speaker-diarization)** | OpenAI 的文件转写加说话人标注模型。 | 可用目标参考声音做云端对照；片段结束后才确定说话人。 |
| **[MOSS-Transcribe-Diarize](https://github.com/OpenMOSS/MOSS-Transcribe-Diarize)** | 联合产生文字与匿名说话人标签的模型。 | 测试“一次完成识字和分人”是否有优势；仍须匹配目标身份。 |
| **[DiCoW / SE-DiCoW](https://github.com/BUTSpeechFIT/TS-ASR-Whisper)** | 让识字模型更关注指定说话人的研究模型。 | 后续目标人直接识别对照。 |
| **[SoloSpeech](https://github.com/WangHelin1997/SoloSpeech)** | 目标人声音提取模型。 | 后续离线提取质量对照。 |
| **[REAL-T](https://huggingface.co/datasets/REAL-TSE/REAL-T)** | 真实多人对话录音数据集。 | 提供混合录音、目标声音样本和目标文字，供比较方案。 |
| **AliMeeting / AISHELL-4** | 中文多人会议录音数据集名称。 | 用来检查中文多人说话效果；会议结果仍需用餐馆样本复核。 |
| **DEV / EVAL1 / EVAL2** | REAL-T 等数据集中的开发验证集 / 两组最终评测集。 | DEV 用来比较方案和调阈值；EVAL1、EVAL2 留到选定方案后报告结果。 |
| **AutoDL** | 按需租用 GPU 服务器的服务。 | 运行本地电脑可能装不下的模型。 |

名称后面的数字也可以这样读：`16K` 通常指 **16 kHz 音频**；`0.6B`、`8B` 中的 `B` 表示 **十亿个模型参数**，所以 0.6B 约为 6 亿、8B 约为 80 亿。“参数数量”是模型规模的一种说法，不等于模型文件大小、显存占用或识别准确率。

## 先用一个餐馆例子看懂问题

你佩戴助听器，在餐馆和一位戴着领夹麦克风的朋友聊天。朋友说“晚上八点在这里见”，你同时说“我听不太清”。目标麦克风以朋友的声音为主，但仍可能录进你的话、邻桌人声和餐具声；系统要把朋友的话显示给你看。

系统需要依次弄清四件事：

1. **哪里有人说话？** 餐具声、音乐和人声要区别开。
2. **谁在说话？** 对话对象的声音，与你和邻桌人的声音要区别开。
3. **说了什么？** 尽量还原对话对象说的“晚上八点在这里见”，尤其是在两人同时说话时。
4. **何时显示？** 尽量在对方说完后约 2–3 秒内，把可靠的文字显示出来。

可以把整个过程记作：**录音 → 找到或认出对话对象的话 → 转成文字 → 显示**。

前面的方案提出了三种实现路径：

| 路径 | 用大白话说 | 能得到什么 |
| --- | --- | --- |
| **先取出对方的声音，再认字** | 尝试从混合录音里“拉出”对方的一条独立音轨，然后转文字 | 目标音轨和文字；可人工试听音轨 |
| **先认出每个人的话，再挑对方的** | 给每段话标出说话人和时间，再将匿名说话人匹配到对方的预录声音 | 带说话人标签的文字；同时说话时可能漏字 |
| **直接听对方的话并认字** | 同时给模型混合录音和对方的声音样本，让它直接输出对方说的字 | 目标文字；可能没有可单独试听的目标音轨 |

这里的“认出每个人的话”是测试目标，不是普通语音识别天然具备的能力。两人同时说话时，它可能只听懂其中一人。

## 最值得先认识的核心词

| 词 | 英文或缩写 | 一句话理解 |
| --- | --- | --- |
| **目标说话人** | target speaker | 这次系统要转写的特定人；主场景中是佩戴领夹麦克风的对话对象。 |
| **声纹** | voiceprint | 从声音中提取、用于判断“是不是这个人”的特征；不等于声音密码或绝对身份证明。 |
| **注册声音** | enrollment audio / reference audio | 预先录好的目标人声音样本，供系统比较。这里的“注册”只是提供样本。 |
| **语音识别** | ASR, Automatic Speech Recognition | 把声音变成文字，即“听懂说了什么”。 |
| **转写** | transcription | 语音识别产出的文字；“晚上八点”就是转写结果。 |
| **语音转文字** | STT, Speech-to-Text | 与 ASR 在本项目中通常指同一件事：把说话声变成文字。 |
| **分说话人** | diarization | 标记“谁在什么时候说话”，但只会先给出匿名编号。 |
| **语音分离** | speech separation | 尝试把混在一起的几个人声变成几条音轨。 |
| **目标说话人提取** | TSE, Target Speaker Extraction | 给定目标人的注册声音，只提取这个人的音轨。 |
| **目标人直接识别** | TS-ASR, Target-Speaker ASR | 给定目标人声音，直接从混音输出这个人的文字。 |
| **目标缺席** | target absent | 录音里目标人完全没说话；此时不应把旁人的话当作目标结果。 |

## 声音和身份：这些词各管一件事

| 术语 | 新手解释 | 餐馆例子或容易混淆的地方 |
| --- | --- | --- |
| **混音 / 混合录音**，mixture | 麦克风收到的所有声音叠在同一个录音里。 | 对方说话、你说话、邻桌人声和餐具声都可能进入目标麦克风。 |
| **音轨**，audio track | 一条可以单独播放的声音流。 | 如果成功分离，可分别试听对方与你的音轨。 |
| **波形**，waveform | 声音在时间轴上的数字表示，是音轨的具体数据。 | 模型输出了波形，才有机会保存、试听目标声音。 |
| **重叠说话**，overlap / overlapping speech | 两人或多人在同一时间说话。 | 对方说“晚上八点”，你同时说“听不清”。比轮流说话难。 |
| **交替发言**，turn-taking | 大家先后说话，重叠较少。 | 对方说完，你才说。通常较容易确定是谁说的。 |
| **旁人内容泄漏**，speaker leakage | 本应只显示对方的内容，却混进了你或邻桌人的字句。 | 结果变成“晚上八点，我听不清”。 |
| **说话人日志 / 分说话人**，speaker diarization | 在时间轴上标注“说话人 A 在几秒到几秒说话”。 | 它回答“谁在何时说”，本身不能恢复被盖住的字。 |
| **说话人标签**，speaker label | 对录音中不同声音的编号。 | `speaker_0` 仅是本段录音中的“第 0 个人”，还不知道是不是对话对象。 |
| **说话人聚类**，speaker clustering | 把听起来属于同一个人的片段归成一组。 | 两次“帮我……”可能被归到同一匿名编号。 |
| **说话人验证**，speaker verification / SV | 比较一段声音和注册样本，判断是否可能来自同一人。 | 用来检查提取音轨是不是对话对象；验证也会误判。 |
| **说话人识别**，speaker identification | 从已知人员里判断是谁。 | “更像甲还是乙”；与仅判断“像不像甲”的验证任务有区别。 |
| **目标说话人活动检测**，personal VAD / pVAD | 持续判断“目标人此刻有没有说话”。 | 可以减少旁人单独说话时误开识别；不能把重叠音频里的旁人声音去掉。 |
| **普通语音活动检测**，VAD, Voice Activity Detection | 判断“此刻有没有人声”，通常不区分是哪位说话人。 | 旁人一直聊天时，普通 VAD 也可能持续认为有人说话。 |
| **说话人增强**，speaker enhancement | 泛指系统优先关注目标人的能力。 | 可能只改变系统的判断，不一定产生可保存的目标音轨。 |
| **背景降噪**，noise suppression | 尽量削弱音乐、风扇、餐具声等干扰。 | 通常不能可靠地把另一个正在说话的人当“噪声”去掉。 |
| **回声消除**，AEC, Acoustic Echo Cancellation | 降低设备扬声器声音又被麦克风录回来的回声。 | 手机播放提示音时可能有用；不是说话人分离。 |
| **自动音量控制**，AGC, Automatic Gain Control | 自动调整录音音量。 | 让远处说话不至于过小；不会判断谁是目标人。 |
| **声纹向量**，embedding / d-vector | 把一段声音压缩为一串数字，供机器比较身份。 | 像“声音特征坐标”，不是可听的录音，也不是人的实名。 |
| **相似度**，similarity / cosine similarity | 比较两个声纹向量有多接近的分数。 | 分数高通常更像同一人，但高分不等于 90% 的身份概率。 |
| **阈值**，threshold | 超过或低于某个分数就作出决定的界线。 | 门槛设高可减少认错旁人，也可能漏掉对话对象。 |
| **校准**，calibration | 用实际样本选择合理阈值，并检验分数对应的错误风险。 | 在安静房间选的阈值，到了嘈杂餐馆可能不合适。 |
| **拒识 / 未知**，rejection / unknown | 系统不确定是目标人时，不强行认作目标。 | 旁人说“订位”，应该可以输出“非目标或不确定”。 |
| **目标在场 / 目标缺席**，target present / target absent | 目标人是否真的在当前录音里说话。 | “缺席”测试专门检查会不会把旁人的话误接收。 |
| **身份判断**，identity decision | 判断这句话是不是目标人说的。 | 与“这句话说了什么”是两项不同的判断；本项目都要做对。 |

## 音频格式与时间：数字究竟表示什么

| 写法 | 含义 | 本项目中的位置 |
| --- | --- | --- |
| **Hz / kHz** | 每秒采样多少次；1 kHz = 1,000 Hz。 | `16 kHz` 表示每秒保存 16,000 个采样点。 |
| **ms**，millisecond，毫秒 | 千分之一秒；1,000 ms = 1 秒。 | “每 160 ms 更新一次”即每 0.16 秒更新一次。 |
| **16-bit** | 每个采样点用 16 位数字表示；与模型“16B 参数”不是一回事。 | 当前输入使用 16-bit 音频。 |
| **PCM**，Pulse-Code Modulation | 未压缩的数字声音采样方式。 | 项目的基础音频输入格式。 |
| **s16le** | 有符号的 16 位整数，字节按“小端”顺序排列。 | `pcm_s16le` 是计算机怎样存放声音数据的具体约定。 |
| **WAV** | 常见音频文件容器。 | WAV 文件内可以装 PCM；“WAV”和“PCM”不是同一层概念。 |
| **单声道 / 多声道**，mono / multichannel | 一个音频通道 / 多个同步通道。 | 项目目前是单声道；多麦克风算法需要保留多个通道。 |
| **重采样**，resampling | 把录音的采样率转换成模型要求的采样率。 | 例如把麦克风录制的 48 kHz 转成 16 kHz。 |
| **分片**，chunk | 把连续录音切成小段送给模型。 | “每 100 ms 一片”指每 0.1 秒送一块音频。 |
| **窗口 / 滑动窗口**，window / sliding window | 模型每次参考的一段近期音频，随后向前滑动。 | 2.4 秒窗口可每 160 ms 更新一次；不表示每次一定等待 2.4 秒。 |
| **缓存 / 缓冲**，buffer | 临时保留尚未处理或尚未输出的录音。 | 可以保留说话开头，防止检测到人声后才录导致漏掉第一个字。 |
| **因果模型**，causal model | 推断当前结果时不依赖未来的声音。 | 有利于在线处理，但代码还需实际支持持续送入和输出音频。 |
| **流式 / 在线**，streaming / online | 录音尚未结束，就持续接收并产生阶段性结果。 | “每 160 ms 出一次目标活动分数”属于持续更新；不等于文字已经最终可用。 |
| **离线 / 整段**，offline / whole-clip | 先拿到一段完整录音，再处理它。 | 用户允许几秒延迟时，可先测这种方式的质量。 |
| **实时**，real-time | 在规定时限内跟上声音的产生；具体允许多少延迟必须说明。 | 算得比录音播放快，也不保证“说完后几秒可显示”。 |
| **端点检测**，endpointing | 判断一句话何时开始、何时说完。 | 餐馆旁人不断讲话，系统可能迟迟等不到“安静”。 |
| **时间戳**，timestamp | 文字或说话人片段在原录音中的起止时间。 | “8.2–9.5 秒由目标人说”用于文字与身份对齐。 |
| **波束形成**，beamforming / MVDR | 利用多个麦克风的空间差异突出某个方向的声音。 | 如果录制时已经混成单声道，就失去了多麦所需的信息。MVDR 是其中一类方法。 |
| **近讲 / 远场**，close-talk / far-field | 麦克风离嘴近 / 离人较远。 | 耳麦通常是近讲；餐桌上的手机常是远场，混响和旁人干扰更明显。 |

## 测试结果：怎样判断方案好不好

| 指标或说法 | 新手解释 | 阅读结果时看什么 |
| --- | --- | --- |
| **正样本 / 负样本**，positive / negative sample | 在某项测试中，应该接受的是正样本，应该拒绝的是负样本。 | 测目标身份时，“对话对象在场并说话”为正样本，“只有你或邻桌人说话”为负样本。 |
| **目标缺席拒识率**，negative rejection rate | 在“目标没说话”的录音中，有多少被正确拒绝。 | 假设 100 条缺席录音中拒绝 75 条，拒识率为 75%；剩下 25 条仍可能误输出。 |
| **误接受 / 错误放行**，false acceptance | 旁人的声音被当成目标人的声音。 | 在这个项目里，你或邻桌人的话被当作对话对象的话显示，尤其要关注。 |
| **误拒绝 / 漏接收**，false rejection | 目标人真的说话，系统却没接受。 | 把声纹阈值调得很严格，误接受可能减少，误拒绝可能增加。 |
| **召回率**，recall | 本应找出的目标内容中，实际找出了多少。 | 10 句真实对话对象发言找回 8 句，目标发言召回率是 80%。 |
| **准确率 / 精确率**，accuracy / precision | 准确率通常指所有判断中有多少判断正确；精确率指“判成目标”的结果里有多少真是目标。 | 两者分母不同；报告指标时写清计算方法，不要只说“准确率高”。 |
| **字错率**，CER, Character Error Rate | 把识别文字与人工正确文本按“字”比较，统计替换、漏字、多字的错误程度。越低越好。 | “晚上八点”认成“晚上九点”有一个字错。只统计非空结果会掩盖整句漏识。 |
| **词错率**，WER, Word Error Rate | 像 CER 一样比较，但以“词”为单位。 | 中英文分词方式不同，不宜直接把 CER 与 WER 的数字相比较。 |
| **说话人日志错误率**，DER, Diarization Error Rate | 衡量说话人时间轴标错、漏标等错误。 | DER 低，不代表说出的文字就一定对。 |
| **重叠区目标字错率** | 只看两人同时说话部分的目标文字错误。 | 专门衡量本项目最难的场景。 |
| **旁人内容泄漏率** | 目标输出里混进了多少旁人的内容。 | 目标 CER 低，仍可能在文字里附带旁人的一句话。 |
| **空结果**，empty output | 模型没有输出文字或音轨。 | 目标缺席时可能正确；目标在场时可能代表漏识。 |
| **基线**，baseline | 用于比较的起点方案。 | 原始混音直接送 ASR、PS4 原版，都可以分别作为基线。 |
| **消融对照**，ablation | 在其他条件尽量相同的情况下，打开或关闭一个组件，看它带来什么差别。 | 同一条音频、同一模型，只比较是否提供目标声纹。 |
| **固定 ASR** | 不同前处理方案使用同一个识字模型。 | 才能较清楚地判断收益来自“提取人声”，还是换了识字模型。 |
| **泛化**，generalization | 在没见过的新场景、新人、新设备上仍能工作。 | 会议录音有效，不代表餐馆手机录音也有效。 |
| **合成混音** | 人工将几段人声和噪声叠在一起制作测试音频。 | 方便精确控制谁更响；还需用真实餐馆录音检验。 |
| **SNR**，Signal-to-Noise Ratio，信噪比 | 目标人声相对餐具声、音乐等背景噪声有多强。 | SNR 越低，一般越难听清目标内容。 |
| **SIR**，Signal-to-Interference Ratio，信号干扰比 | 目标人声相对其他说话人的声音有多强。 | “旁人比目标更响”主要改变 SIR，不应与背景噪声的 SNR 混为一谈。 |
| **dB**，decibel，分贝 | 声音强弱比值的单位。 | 目标对旁人的 SIR 为 `-5 dB`，表示目标比干扰人声弱；不是“负音量”。 |
| **SI-SDR**，Scale-Invariant Signal-to-Distortion Ratio | 有“真正干净目标音轨”作标准答案时，衡量提取后音轨失真程度的指标。 | REAL-T 的真实混音没有这种干净标准音轨，不能直接用它算有参考的 SI-SDR。 |
| **延迟**，latency | 从指定起点到拿到结果所用时间。 | 必须说明是“开始说话到首字”，还是“对方说完到文字显示”。 |
| **p50 / p95** | 一批测量结果的中位值 / 95% 的结果不超过的值。 | p95 能显示大多数用户中较慢的一批请求；只看平均值可能漏掉卡顿。 |
| **RTF**，Real-Time Factor，实时系数 | 处理时间除以录音时长。 | 10 秒录音用 3 秒处理，RTF = 0.3；若还要等录完、上传、判身份，实际等待会更长。 |
| **冷启动 / 预热 / 稳态**，cold start / warm-up / steady state | 第一次加载模型 / 提前运行让模型就绪 / 加载完成后的正常运行。 | 第一次请求与持续运行的耗时应分别报告。 |
| **显存**，VRAM | 显卡存放模型和中间计算结果的内存。 | 权重文件 35 GB，并不直接等于运行时显存需求。 |
| **峰值显存**，peak VRAM | 一次处理过程中占用显存的最大值。 | 决定某张 GPU 是否能装下模型及其推理中间结果。 |
| **每小时旁人误显示次数** | 连续运行时，平均每小时有多少次把用户或旁人的话显示成目标人转写。 | 比几段短录音上的成功率更贴近日常使用。 |

一个关键的比较方法是：**先让各方案找回差不多数量的对话对象发言，再比较谁更少显示你或邻桌人的话**。如果一个方案把所有人都拒绝，它的旁人误显示率会很低，但也没有实际用途。

## 看模型仓库时常见的英文

| 词 | 通俗含义 | 为什么会遇到 |
| --- | --- | --- |
| **模型**，model | 从大量样本学到规律、能对新声音作出预测的程序。 | PS4、Xiaomi-CocktailASR-1 都是模型或模型系统的名称。 |
| **参数**，parameters | 模型内部通过训练得到的大量数字。 | “8B 参数”是约 80 亿个数字的规模描述。 |
| **权重**，weights | 保存模型参数的文件。 | 想在 AutoDL 自己运行公开模型，通常要先取得权重文件。 |
| **检查点**，checkpoint / ckpt | 某次训练保存下来的模型状态，通常包括权重。 | `checkpoint epoch 37` 指训练到某个阶段时保存的版本。 |
| **预训练**，pretrained | 已经在大量数据上训练过，可直接试用或继续改进。 | 不必从零训练一个目标说话人模型。 |
| **训练 / 微调**，training / fine-tuning | 从数据学习参数 / 在已有模型基础上继续学习。 | 比“下载权重然后测试”更费数据和算力。 |
| **推理**，inference | 用已训练模型处理新录音并得到结果。 | “一次推理耗时 3 秒”是处理录音的运行时间。 |
| **端到端**，end-to-end | 从原始输入直接得到最终结果，中间步骤可能由一个系统一起完成。 | TS-ASR 直接输出目标文字；仍需检查是否认错人。 |
| **条件化**，conditioned on reference | 把目标参考声音作为模型的额外条件。 | 同一段混音，换一位目标人的参考音，理想情况下输出也应切换。 |
| **盲分离**，blind separation | 分离时先不指定要哪位说话人。 | 两轨分离模型输出 A、B 两轨后，还要做声纹选轨。 |
| **选轨**，track selection | 在已分出的音轨里选出目标人的那条。 | 也必须允许“没有任何一条是目标人”。 |
| **CoT**，Chain of Thought | 模型额外生成分析步骤的模式。 | 初测小米模型时可先关闭，单独测基本识别与耗时。 |
| **LLM**，Large Language Model，大语言模型 | 擅长处理和生成文字的模型。 | 例如 Xiaomi-CocktailASR-1 内部用 Qwen3-8B 根据声音信息生成目标文字；单靠文字不能判断说话人身份。 |
| **开源代码 / 开放权重**，open-source code / open weights | 能看到或下载程序代码 / 能取得模型参数文件。 | 两者要分别确认；能下载也要看使用许可。 |
| **许可**，license | 规定代码、权重或数据可怎样使用的条款。 | 有些模型允许研究测试，但商业使用有限制。 |
| **GitHub** | 常见的代码托管网站。 | 查看模型实现、说明文档和版本。 |
| **Hugging Face / ModelScope** | 常见的模型与数据集发布平台。 | 下载模型权重或测试录音。 |
| **仓库**，repository / repo | 一套项目文件及其版本记录。 | “WeSep 主仓库”和“REAL-TSE 专用仓库”是两套发布位置。 |
| **README** | 仓库首页常见的“先读我”说明文件。 | 查看安装方法、模型用途和局限。 |
| **研究预览版**，research preview | 开发者公开了研究中的版本，功能和接口可能尚未齐备。 | 不等于已经提供可直接部署的完整服务。 |
| **Python / PyTorch** | 常见编程语言 / 用来运行和训练模型的软件框架。 | 模型的示例代码常用它们。 |
| **CPU / GPU** | 通用处理器 / 擅长大规模并行计算的处理器。 | 语音大模型常借助 GPU 加速。 |
| **CUDA / RTX 3090** | NVIDIA GPU 的计算软件环境 / 一款 NVIDIA 显卡型号。 | 现有 PS4 测试曾在 RTX 3090 上运行。 |
| **云端 / 本地**，cloud / local | 在远程服务器运行 / 在自己的电脑或手机运行。 | AutoDL 是租用云端 GPU；需要上传录音才能远程处理。 |
| **API**，Application Programming Interface | 一个程序调用另一个程序或服务的接口。 | Qwen 和 OpenAI 服务都可通过 API 处理录音。 |
| **URL / HTTPS** | URL 是网络地址；HTTPS 是常见的加密网页传输方式。 | Qwen 的声纹字段需要服务端能够访问的音频 URL。 |
| **API Key** | 调用云服务时用于识别账号的密钥。 | 放在私有配置里，不能写进公开仓库。 |
| **WebSocket** | 客户端与服务端维持持续连接的通信方式。 | Qwen 实时探针持续发送音频分片并接收事件。 |
| **CLI**，Command-Line Interface | 在终端输入命令操作程序。 | `voice-focus-asr probe-qwen` 是本项目的命令行工具。 |
| **探针**，probe | 为回答一个具体问题而写的小型试验程序。 | 当前 Qwen 探针验证“提供声纹后会怎样输出”，不是完整产品。 |
| **适配器**，adapter | 把某个模型特有的输入输出接到项目统一接口上的代码。 | 换模型时尽量改适配器，而不改整条业务流程。 |
| **接口 / 端口 / 契约**，interface / port / contract | 组件间预先约定“接受什么、返回什么、按什么规则调用”。 | 项目规定音频是 16 kHz 单声道 PCM，处理器可被不同模型替换。 |
| **会话**，session | 从开始注册目标、处理录音，到结束的一次完整运行。 | 一次餐馆录音任务对应一个聚焦转写会话。 |

## 项目代码中出现的名字

读代码时，可以把这些名字当作分工，而不是另一批语音模型：

| 代码或协议名称 | 它在做什么 |
| --- | --- |
| `AudioChunk` | 一小块按顺序传入的录音。 |
| `TargetSpeakerProcessor` | “先关注谁”的组件接口；可以筛选或提取目标人声音。 |
| `StreamingRecognizer` | “把音频变成字”的组件接口。 |
| `TranscriptUpdate` | 一次文字更新；可能是会修改的初步结果，也可能是最终结果。 |
| `speaker_label="target"` | 当前转写默认贴“目标人”标签；真正做全员转写时还需支持“未知”和匿名人编号。 |
| `process()` | 处理一块录音的函数。当前返回音频或 `None`；`None` 在现有会话里表示这一块被过滤。 |
| `finish()` | 告诉组件录音结束，处理剩余内容。目标提取模型可能此时还有缓冲音频需要交付。 |
| `smart_turn` | Qwen 服务的一种“判断一句话是否构成有效发言”的模式；并不等同于身份证明。 |
| `voiceprint_audio_urls` | Qwen API 中传入目标人预录声音网址的字段。 |
| `accepted` | 试验报告里“进入有效转写”的结果分类。 |
| `ambient transcription` | Qwen 将听到的某些内容放在环境声音转写通道；出现这个事件不能单独证明“声纹正确拒绝了旁人”。 |
| `turn_invalid` | Qwen 认为一次发言轮次无效的事件原因；需结合前后事件判断最终有没有接受。 |
| `diarized_json` | 带说话人标签和起止时间的 JSON 结果格式。JSON 是一种机器易读的文本格式。 |
| `stream=true` | 某些 API 边产生结果边发送；对文件转写而言，不表示已支持持续麦克风输入。 |
| `config.local.toml` | 本项目放本地配置的文件；TOML 是一种配置文件格式。 |
| `api_key` / `DASHSCOPE_API_KEY` | 配置字段 / 环境变量，用来提供云服务密钥。环境变量是运行程序时传给它的设置。 |

## 六组容易混淆的说法

1. **“有人说话”与“目标人在说话”**：普通 VAD 只回答前者；目标活动检测还尝试回答后者。
2. **“知道谁在什么时间说话”与“听清他说了什么”**：分说话人与转写是两项不同工作。匿名标签也还没有对应到对话对象。
3. **“分出两条音轨”与“知道哪条是对话对象的”**：盲分离之后仍需声纹选轨，并允许目标不在场。
4. **“分数高”与“可以放心显示”**：声纹分数和识字结果都可能出错，须分别测试。
5. **“模型算得比录音快”与“用户等得短”**：还要计入录音长度、何时判定说完、上传和结果返回。
6. **“会议数据表现好”与“餐馆里也好”**：录音设备、距离、说话人、混响和噪声不同，需要实地样本验证。

读[方案分析](./docs/experiments/target_speaker_options_2026-09-22.md)时，可以先只关注三项结果：**有没有保住目标人的话、有没有放行旁人的话、说完后要等多久**。其他指标大多是为了找出这三项表现背后的原因。

## 项目领域约定

以下定义承接本文开头的主场景。编写代码、协议和其他文档时，以这些名称和关系为准。

### 目标说话人

需要把话转成文字的已知说话人。主场景中是用户的对话对象，通常佩戴目标麦克风，并通过预注册的干净语音建立声纹。

避免使用“当前用户”作为同义词：目标说话人恰恰不是设备持有者。

### 用户

查看转写文字的人，也就是佩戴助听器的一方。用户的语音不需要显示，是近场场景中最主要、且始终在场的干扰源。

避免把用户称为“目标”或“说话人 A/B”；在需要区分时使用“目标说话人”和“用户”。

### 目标声纹样本

用于表征目标说话人身份的预录音频。它只用于说话人验证、目标说话人提取或目标说话人语音识别，不作为 ASR 训练数据。

不同模型对样本长度有不同要求，例如部分 TS-ASR 模型只使用 1–4 秒参考音频，因此样本应准备成可直接截取的干净短片段。

### 用户声纹样本

用户本人的预录音频，作为**负样本**使用：判断一段语音“更像目标说话人还是更像用户”，比只和目标声纹做单类比对更稳。它与目标声纹样本适用相同的保存期限和删除要求。

### 目标麦克风与参考麦克风

- 目标麦克风：目标说话人佩戴的近场麦克风，是转写的主输入声道。
- 参考麦克风：用户佩戴的近场麦克风（或手机麦克风），可选。它不用于转写，只为说话人活动判定和串音消除提供参考。

两者经同一接收器以多声道方式同步输入时，声道与说话人的对应关系属于会话配置，不能靠猜测。

### 近场与远场

- 近场：麦克风距离说话人嘴部约 30 cm 以内，目标语音明显强于其他声源。
- 远场：麦克风距离所有说话人都较远，例如桌面手机或会议阵列，各声源强度接近，重叠语音难以处理。

同一模型在近场和远场的表现可能差异很大，评测结果必须注明拾音条件。

### 串音（Crosstalk）

非佩戴者的声音进入某人的近场麦克风。主场景中主要指用户的声音进入目标麦克风。串音通常比佩戴者语音弱，但足以被 ASR 转写出来。

### 说话人活动判定

判断某个时间段内是谁在说话：目标说话人、用户、两人同时或无人。可以依据目标/参考麦克风的能量比、声纹比对或二者结合。它的输出用于门控，不产生音轨。

### 说话人日志（Speaker Diarization）

判断“谁在什么时候说话”，输出说话人标签和时间区间。它不保证从重叠语音中恢复独立波形，标签也不自动对应到已注册的身份。

避免把 diarization 称为“独立音轨分离”。

### 语音分离（Speech Separation）

从混合波形估计多个独立说话人波形。输出是一个或多个音轨，不等同于说话人身份识别。

### 目标说话人提取（Target Speaker Extraction, TSE）

根据目标声纹样本，从混合语音中恢复目标说话人的波形。它适合处理重叠语音，但不保证在目标缺席时输出静音，因此输出仍需说话人验证。

### 目标说话人语音识别（Target-Speaker ASR, TS-ASR）

以目标声纹样本为条件，直接从混合语音输出目标说话人的文字，不产生中间音轨。例如 Xiaomi-CocktailASR-1。

它同时承担身份筛选与识别，因此与“先 TSE 再 ASR”不同：无法单独评测或保存筛选后的音频，识别错误与筛选错误需要通过对照实验区分。

### 说话人验证（Speaker Verification）

比较一段语音与声纹样本的 embedding 相似度，判断是否为同一人。它是“目标缺席时不输出”的最终保障，可用于门控原始音频、复核 TSE 输出或筛选转写段。

### 说话人增强（Speaker Enhancement）

云端或本地系统对目标说话人进行优先检测并抑制旁人及背景噪声的能力。它可能只影响识别或轮次判断，不保证输出可保存的独立音轨。

### 拒识与目标缺席

- 目标缺席：一段音频中不含目标说话人语音，例如只有用户在说话或只有环境声。
- 拒识：系统在目标缺席时不输出文字。
- 误拒：目标说话人确实在说话，系统却没有输出。

拒识率和误拒率必须同时报告，单看其中一个没有意义。

### 流式语音识别

持续接收音频分片，并产生可修订的增量文本和不可再修改的最终文本。

在允许 2–3 秒延迟的前提下，“按 VAD 分段后整段识别”和“先出增量文本、段末再修正”都属于可接受的实现，不要求逐帧流式模型。

### 显示延迟

从目标说话人一句话说完，到对应最终文本出现在显示设备上的时间。它包括采集、网络传输、处理、识别和回传，是面向用户的主延迟指标。

### 聚焦转写会话

从声纹样本准备开始，经过音频处理和识别，直到正常完成或中止的一次完整运行。

### 评测指标

- 目标 CER：目标说话人文本的字错误率。
- 用户泄漏率：用户说的话被显示出来的比例。
- 目标缺席误接受率：目标缺席片段中产生文字的比例，即 1 − 拒识率。
- 误拒率：目标语音片段没有产生文字的比例。
- 显示延迟：见上文。

### 关系

- 一个聚焦转写会话必须对应一个目标说话人，可以额外关联一个用户声纹样本。
- 一个目标说话人可以拥有多个目标声纹样本。
- 目标麦克风是必需输入；参考麦克风是可选输入。
- 身份筛选可以发生在识别之前（说话人活动判定门控、TSE）、识别之中（TS-ASR）或识别之后（按转写段做说话人验证）；它们可以叠加使用。
- 任何不能保证拒识的环节（TSE、TS-ASR、说话人增强）之后，都应有说话人活动判定或说话人验证兜底。
- 说话人日志可以作为观测或评测能力存在，但不能替代目标说话人提取或说话人验证。
- 降噪、回声消除和 VAD 是音频前处理，不拥有转写会话状态；降噪是否提升识别准确率必须通过对照实验确认。
